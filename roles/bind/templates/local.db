$TTL 60
$ORIGIN local.
@ IN SOA dns1.local root.local. (
        1 ; serial
               30M ; refresh
               15M ; retry
                1W ; expiry
                1M ; nx ttl
)

{% for host in groups['dns']|sort %}
@ NS {{ hostvars[host]['host_fqdn'] }}.
{% endfor %}

{% for host in groups['all']|sort %}
{{ hostvars[host]['inventory_hostname'] }} A {{ hostvars[host]['primary_ip'] }}
{% endfor %}

